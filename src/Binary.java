public abstract class Binary implements Expression {

    // Member variables of the Binary objects
    // left_ and right_ are the two branches of every leaf of the tree
    Expression left_;
    Expression right_;
}